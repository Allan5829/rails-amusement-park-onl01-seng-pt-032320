<%= form_for :user, url: '/signedin' do |f| %>
    <%= f.label :name %>
    <%= f.collection_select :name, User.all, :name, :name %>
    <br/>
     
    <label>Password</label>
    <input type="text" id="password" name="user[password]"><br>

    <%= f.submit "Sign In" %>
<% end %>
